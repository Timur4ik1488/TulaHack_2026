# Docker / compose: задайте DATABASE_URL или POSTGRES_* (см. корневой .env).
# DATABASE_URL=postgresql+asyncpg://USER:PASSWORD@db:5432/DBNAME
# Локально к контейнеру БД: POSTGRES_HOST=127.0.0.1 POSTGRES_PORT=5434

SECRET_KEY=change-me
JWT_SECRET_KEY=change-me-too
