# Docker / compose: задайте DATABASE_URL или POSTGRES_* (см. корневой .env рядом с compose.yml).
# DATABASE_URL=postgresql+asyncpg://USER:PASSWORD@HOST:PORT/DBNAME
# Локальный Postgres в compose (сервис db): POSTGRES_HOST=db POSTGRES_PORT=5432
#
# --- DEPLOY: удалённая БД (не активировать на локалке; скопировать в .env при деплое) ---
# Хост: 150.241.103.30 , порт PostgreSQL: 5435
#POSTGRES_HOST=150.241.103.30
#POSTGRES_PORT=5435
#POSTGRES_USER=
#POSTGRES_PASSWORD=
#POSTGRES_DB=
# Или одной строкой:
#DATABASE_URL=postgresql+asyncpg://USER:PASSWORD@150.241.103.30:5435/DBNAME

SECRET_KEY=change-me
JWT_SECRET_KEY=change-me-too

# Telegram (internal API + бот; см. корневой .env и compose.yml → сервис tg_bot)
# TELEGRAM_INTERNAL_SECRET=тот-же-секрет-что-в-корневом-.env
# TELEGRAM_BOT_TOKEN=
# PUBLIC_SITE_URL=http://localhost
