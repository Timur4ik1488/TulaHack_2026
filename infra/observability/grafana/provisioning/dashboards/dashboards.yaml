apiVersion: 1

providers:
  - name: HackSwipe
    orgId: 1
    folder: HackSwipe
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/json
